name: github pipeline

on: push

jobs:
  run-app:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name:
        run: ls -R
      - name : Run python file
        run: python app.py 
        
