name: github pipeline

on: push

jobs:
  run-app:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v3
      - name:
        run: ls 
      - name : Run python file
        run: python pratice/app.py 
        
